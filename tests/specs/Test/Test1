


const sharedStepAction = function(stepName) {
  const funcDictionary = [
       
  ];

  eval(funcDictionary.find((dictElement) => { return dictElement.stepName === stepName}).stepFunction)();
}
describe('Test1', () => {
  
  beforeEach(() => {
    browser.url('https://google.com');
  });

  
  
  it('Name1', () => {
    $(ta('Test:Test1:qInput011432101012')).setValue('dsssdssdssq1');
    $(ta('Test:Test1:qInput011432101012')).click();
    $(ta('Test:Test1:btnKInput01143210300')).click();
  });

});
